"""Plastanno — dual-evidence, confidence-aware plastome annotation.

`__version__` is the single source of truth for the version: pyproject.toml reads
it via setuptools' dynamic `attr:`, so the packaged metadata and the running code
can never disagree (a stale egg-info previously made `--version` report an old
release while the code was newer).
"""

__version__ = "2.0.4"
